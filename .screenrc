# no yakkity at the beginning, and I don't like the wuff wuff bit
startup_message off
vbell off

# hack the default ^a to something I type less often
escape ^pp

# unset copy mode - was probably hitting this by accident
#bind ^[
#bind [
